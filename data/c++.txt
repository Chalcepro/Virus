// === Basic Includes and Namespaces ===
#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <set>
#include <algorithm>
#include <fstream>
#include <sstream>
#include <memory>
#include <stdexcept>
#include <cmath>
#include <functional>
#include <thread>
#include <mutex>

using namespace std;

// === Variables and Primitive Types ===
int age = 25;
double price = 19.99;
char grade = 'A';
bool is_active = true;
unsigned int count = 100;
const float PI = 3.14159f;

// === Strings ===
string name = "Alice";
string greeting = "Hello, " + name + "!";
string multiline = R"(This is a
multiline string)";
cout << greeting << endl;
cout << "Length: " << name.length() << endl;
cout << "Substring: " << name.substr(1, 3) << endl;
cout << "Find 'li': " << name.find("li") << endl;

// === Arrays (C-style) ===
int numbers[5] = {1, 2, 3, 4, 5};
int matrix[2][3] = {{1,2,3}, {4,5,6}};

// === Vectors (dynamic arrays) ===
vector<int> vec = {10, 20, 30};
vec.push_back(40);
vec.pop_back();
cout << "Size: " << vec.size() << endl;
for (int v : vec) cout << v << " ";
vector<int>::iterator it = find(vec.begin(), vec.end(), 20);
if (it != vec.end()) cout << "Found" << endl;
sort(vec.begin(), vec.end(), greater<int>());

// === Pairs and Tuples ===
pair<string, int> person = make_pair("Bob", 30);
cout << person.first << " " << person.second << endl;
tuple<int, double, string> tup = make_tuple(1, 2.5, "test");
cout << get<0>(tup) << endl;

// === Functions ===
int add(int a, int b) {
    return a + b;
}
void printMessage(const string& msg) {
    cout << msg << endl;
}
// Function overloading
double add(double a, double b) {
    return a + b;
}
// Default parameters
int multiply(int a, int b = 2) {
    return a * b;
}
// Inline
inline int square(int x) { return x * x; }

// === Lambdas ===
auto sum = [](int a, int b) -> int { return a + b; };
cout << sum(3, 4) << endl;
int factor = 10;
auto scale = [factor](int x) { return x * factor; };
cout << scale(5) << endl;

// === Control Flow ===
if (age >= 18) {
    cout << "Adult" << endl;
} else {
    cout << "Minor" << endl;
}
string category = (age < 13) ? "Child" : (age < 20) ? "Teen" : "Adult";

switch (grade) {
    case 'A': cout << "Excellent"; break;
    case 'B': cout << "Good"; break;
    default: cout << "OK";
}

for (int i = 0; i < 5; ++i) cout << i << " ";
int j = 0;
while (j < 5) { cout << j++ << " "; }
do { cout << --j << " "; } while (j > 0);

// Range-based for (C++11)
for (int v : vec) cout << v << " ";

// === Classes ===
class Animal {
protected:
    string name;
public:
    Animal(const string& n) : name(n) {}
    virtual void speak() const { cout << name << " makes a noise." << endl; }
    virtual ~Animal() {} // virtual destructor
};

class Dog : public Animal {
    string breed;
public:
    Dog(const string& n, const string& b) : Animal(n), breed(b) {}
    void speak() const override { cout << name << " barks!" << endl; }
    string getBreed() const { return breed; }
    static string info() { return "Dogs are loyal."; }
};

// Operator overloading
class Point {
public:
    int x, y;
    Point(int a, int b) : x(a), y(b) {}
    Point operator+(const Point& p) const { return Point(x + p.x, y + p.y); }
    friend ostream& operator<<(ostream& os, const Point& p) {
        os << "(" << p.x << "," << p.y << ")"; return os;
    }
};

// Smart pointers
unique_ptr<Dog> dogPtr = make_unique<Dog>("Rex", "German Shepherd");
shared_ptr<Animal> animalPtr = make_shared<Dog>("Buddy", "Labrador");

// === Templates ===
template <typename T>
T maxVal(T a, T b) { return (a > b) ? a : b; }

template <class T>
class Container {
    T data;
public:
    Container(T d) : data(d) {}
    T get() const { return data; }
    void set(T d) { data = d; }
};

// === STL Containers ===
// Map
map<string, int> ages;
ages["Alice"] = 30;
ages["Bob"] = 25;
for (const auto& [key, value] : ages) {
    cout << key << " is " << value << endl;
}

// Set
set<int> s = {3, 1, 4, 1, 5, 9};
s.insert(2);
cout << "Set size: " << s.size() << endl;

// Deque, list, stack, queue
deque<int> dq = {1,2,3};
dq.push_front(0);
list<int> lst = {1,2,3,4};
lst.push_back(5);
stack<int> st;
st.push(1);
queue<int> q;
q.push(2);

// === Algorithms ===
vector<int> arr = {4, 2, 8, 5, 1};
sort(arr.begin(), arr.end());
auto it_min = min_element(arr.begin(), arr.end());
cout << "Min: " << *it_min << endl;
int cnt = count(arr.begin(), arr.end(), 5);
bool found = binary_search(arr.begin(), arr.end(), 8);
reverse(arr.begin(), arr.end());
random_shuffle(arr.begin(), arr.end()); // deprecated in C++17, use shuffle

// Transform
vector<int> squared(arr.size());
transform(arr.begin(), arr.end(), squared.begin(), [](int x) { return x*x; });

// Copy and move
vector<int> copy_vec(arr);

// === File I/O ===
ofstream outFile("output.txt");
if (outFile.is_open()) {
    outFile << "Hello, file!" << endl;
    outFile.close();
}

ifstream inFile("input.txt");
if (inFile.is_open()) {
    string line;
    while (getline(inFile, line)) {
        cout << line << endl;
    }
    inFile.close();
}

// String streams
stringstream ss;
ss << "The number is " << 42;
string output = ss.str();

// === Exception Handling ===
try {
    throw runtime_error("Something went wrong");
} catch (const exception& e) {
    cerr << "Error: " << e.what() << endl;
}

// Custom exception
class MyException : public exception {
public:
    const char* what() const noexcept override { return "My custom error"; }
};

// === Memory Management (raw pointers) ===
int* p = new int(10);
delete p;
int* arr_ptr = new int[5];
delete[] arr_ptr;

// === Threading (C++11) ===
void worker(int id) {
    cout << "Thread " << id << endl;
}
thread t1(worker, 1);
t1.join();
mutex mtx;
void safePrint(const string& msg) {
    lock_guard<mutex> lock(mtx);
    cout << msg << endl;
}

// === Recursion ===
int factorial(int n) {
    if (n <= 1) return 1;
    return n * factorial(n-1);
}

int fibonacci(int n) {
    if (n <= 1) return n;
    return fibonacci(n-1) + fibonacci(n-2);
}

// === Linked List (manual) ===
struct ListNode {
    int val;
    ListNode* next;
    ListNode(int x) : val(x), next(nullptr) {}
};

ListNode* head = new ListNode(1);
head->next = new ListNode(2);
head->next->next = new ListNode(3);

// Clean up
ListNode* curr = head;
while (curr) {
    ListNode* tmp = curr;
    curr = curr->next;
    delete tmp;
}

// === Sorting Algorithms (common) ===

// Bubble sort
void bubbleSort(vector<int>& arr) {
    int n = arr.size();
    for (int i = 0; i < n-1; ++i)
        for (int j = 0; j < n-i-1; ++j)
            if (arr[j] > arr[j+1])
                swap(arr[j], arr[j+1]);
}

// Quick sort (simplified)
int partition(vector<int>& arr, int low, int high) {
    int pivot = arr[high];
    int i = low - 1;
    for (int j = low; j < high; ++j) {
        if (arr[j] < pivot) {
            ++i;
            swap(arr[i], arr[j]);
        }
    }
    swap(arr[i+1], arr[high]);
    return i+1;
}
void quickSort(vector<int>& arr, int low, int high) {
    if (low < high) {
        int pi = partition(arr, low, high);
        quickSort(arr, low, pi-1);
        quickSort(arr, pi+1, high);
    }
}

// === Number Theory ===
bool isPrime(int n) {
    if (n < 2) return false;
    for (int i = 2; i * i <= n; i++) {
        if (n % i == 0) return false;
    }
    return true;
}

int gcd(int a, int b) {
    return b == 0 ? a : gcd(b, a % b);
}

// === Dynamic Programming (knapsack example) ===
int knapSack(int W, vector<int> wt, vector<int> val, int n) {
    vector<vector<int>> K(n + 1, vector<int>(W + 1));
    for (int i = 0; i <= n; i++) {
        for (int w = 0; w <= W; w++) {
            if (i == 0 || w == 0)
                K[i][w] = 0;
            else if (wt[i - 1] <= w)
                K[i][w] = max(val[i - 1] + K[i - 1][w - wt[i - 1]], K[i - 1][w]);
            else
                K[i][w] = K[i - 1][w];
        }
    }
    return K[n][W];
}

// === String Algorithms ===
bool isPalindrome(const string& str) {
    int left = 0, right = str.size() - 1;
    while (left < right) {
        if (str[left++] != str[right--]) return false;
    }
    return true;
}

int main() {
    cout << "C++ Training Data Sample" << endl;
    return 0;
}